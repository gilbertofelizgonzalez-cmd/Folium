<UserControl x:Class="PdfToolkit.UI.Views.MergeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:PdfToolkit.UI.ViewModels"
             xmlns:models="clr-namespace:PdfToolkit.UI.Models">
    <UserControl.Resources>
        <Style x:Key="InfoBarStyle" TargetType="Border">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#1A1976D2"/>
            <Setter Property="BorderBrush" Value="#331976D2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding InfoBarSeverity}" Value="Success">
                    <Setter Property="Background" Value="#1A2E7D32"/>
                    <Setter Property="BorderBrush" Value="#332E7D32"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding InfoBarSeverity}" Value="Warning">
                    <Setter Property="Background" Value="#1AF57C00"/>
                    <Setter Property="BorderBrush" Value="#33F57C00"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding InfoBarSeverity}" Value="Error">
                    <Setter Property="Background" Value="#1AC62828"/>
                    <Setter Property="BorderBrush" Value="#33C62828"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24" AllowDrop="True" Drop="OnFileDrop" DragOver="OnDragOver">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Unir PDFs" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Opacity="0.7" Margin="0,0,0,16"/>

        <Border Grid.Row="2"
                Style="{StaticResource InfoBarStyle}"
                Visibility="{Binding IsInfoBarOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding InfoBarTitle}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding InfoBarMessage}" TextWrapping="Wrap" Opacity="0.85" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="✕"
                        Command="{Binding CloseInfoBarCommand}"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="8"
                        Margin="8,0,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Row="3" BorderThickness="1" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" CornerRadius="4">
            <ListBox x:Name="FilesListBox"
                     ItemsSource="{Binding SourceFiles}"
                     AllowDrop="True"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     PreviewMouseLeftButtonDown="FilesListBox_PreviewMouseLeftButtonDown"
                     PreviewMouseMove="FilesListBox_PreviewMouseMove"
                     DragOver="FilesListBox_DragOver"
                     Drop="FilesListBox_Drop">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,2"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:PdfFileItem}">
                        <Grid Height="100" Margin="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="⋮⋮"
                                       FontSize="20"
                                       Opacity="0.5"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"
                                       Cursor="SizeAll"/>

                            <Border Grid.Column="1"
                                    Width="80" Height="100"
                                    Background="{DynamicResource SystemControlBackgroundChromeLowBrush}"
                                    CornerRadius="2"
                                    Margin="0,0,16,0">
                                <Grid>
                                    <TextBlock Text="📄"
                                               FontSize="32"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Opacity="0.5"/>
                                    <Image Source="{Binding Thumbnail}"
                                           Stretch="Uniform"/>
                                </Grid>
                            </Border>

                            <TextBlock Grid.Column="2"
                                       Text="{Binding FileName}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="14"/>

                            <StackPanel Grid.Column="3"
                                        Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                <Button Content="▲"
                                        Width="36" Height="36"
                                        Margin="2,0"
                                        ToolTip="Mover arriba"
                                        Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="▼"
                                        Width="36" Height="36"
                                        Margin="2,0"
                                        ToolTip="Mover abajo"
                                        Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="✕"
                                        Width="36" Height="36"
                                        Margin="2,0"
                                        ToolTip="Eliminar"
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="4" Margin="0,16,0,0"
                    Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="0,0,0,4">
                <TextBlock Text="{Binding CurrentFileName, StringFormat='Procesando: {0}'}" Opacity="0.7"/>
                <TextBlock Text="{Binding ProgressPercentage, StringFormat='{}{0:F0}%'}" HorizontalAlignment="Right" Opacity="0.7"/>
            </Grid>
            <ProgressBar Height="6" Value="{Binding ProgressPercentage}" Maximum="100"/>
        </StackPanel>

        <Grid Grid.Row="5" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Agregar PDFs"
                        Command="{Binding SelectFilesCommand}"
                        Style="{StaticResource AccentButtonStyle}"
                        Padding="20,10"/>
                <Button Content="Limpiar lista"
                        Command="{Binding ClearAllCommand}"
                        Padding="20,10"
                        Margin="8,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancelar"
                        Command="{Binding CancelMergeCommand}"
                        Padding="20,10"
                        Margin="0,0,8,0"
                        Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="Unir PDFs"
                        Command="{Binding MergeCommand}"
                        Style="{StaticResource AccentButtonStyle}"
                        Padding="20,10"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
